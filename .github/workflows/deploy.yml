name: Deploy workflow

on:
  workflow_call:
    inputs:
      environment:
        type: string
        required: true
    secrets:
      SERVER_HOST:
        required: true
      SERVER_USERNAME:
        required: true
      SERVER_PASSWORD:
        required: true
      SERVER_PORT:
        required: true

jobs:
  copy_compose:
    environment: ${{ inputs.environment }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

#      - name: Copy file
#        uses: garygrossgarten/github-action-scp@release
#        with:
#          host: ${{ secrets.server_host }}
#          username: ${{ secrets.server_username }}
#          password: ${{ secrets.server_password }}
#          port: ${{ secrets.server_port }}
#          local: ${{ inputs.source }}
#          remote: ${{ inputs.target }}


      - name: Dump Password
        run: |
          echo Password is $HOST
          if [[ $HOST = *"digital.merphi.ru"* ]]; then
            echo "This is the correct host!"
          else
            echo "This is NOT the correct host!"
          fi
        env:
          HOST: ${{ secrets.SERVER_HOST }}

      - name: Restart containers
        uses: garygrossgarten/github-action-ssh@release
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USERNAME }}
          password: ${{ secrets.SERVER_PASSWORD }}
          port: ${{ secrets.SERVER_PORT }}
          command: cd docker
            #sudo docker compose -f docker-compose-server.yaml up -d